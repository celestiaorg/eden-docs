# React wallet panel

import { Callout } from 'nextra/components'

<Callout type="info" emoji="🚧">
  This page is under construction.
</Callout>

React wallet panel is a React/TypeScript component with Privy (auth + embedded wallets), ZeroDev (AA smart accounts), and Eden (TODO coming soon).

## Installation

```bash copy
# TODO finalize this, for now must use local version:
# for now: use npm link to create local bypass
npm install @wallet-panel/React
```

Install peer dependencies:

```bash copy
npm install react react-dom @privy-io/react-auth @zerodev/waas wagmi viem
```

## Usage

```tsx copy
import { WalletPanel } from '@wallet-panel/react'

function App() {
  return <WalletPanel privyClient={privyClient} onRequestLogin={() => openPrivyLogin()} />
}
```

## Usage with ZeroDev (smart accounts)

```tsx copy
import { WalletPanel } from '@wallet-panel/react'

function App() {
  return (
    <WalletPanel
      privyClient={privyClient}
      zerodev={zerodevContext}
      enableSponsoredTx={true}
      onRequestLogin={() => openPrivyLogin()}
      onTxSubmitted={hash => console.log('Transaction:', hash)}
    />
  )
}
```

## Demo

Fill in the [`.env.local` file](https://github.com/gbarros/react-wallet-UI/blob/main/apps/demo/.env.example) with your Privy and ZeroDev API keys. Optionally, you can configure RPC URLs or a WalletConnect project ID.

Run the [demo](https://github.com/gbarros/react-wallet-UI?tab=readme-ov-file#demo) yourself to see the wallet panel in action.

## Learn more

See [the repository](https://github.com/gbarros/react-wallet-UI) to learn more about how to use react wallet panel with just Privy or ZeroDev, customize chains, tokens, and localization.
