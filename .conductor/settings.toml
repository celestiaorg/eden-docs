"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bun install --frozen-lockfile"
run_mode = "concurrent"

[scripts.run.deploy-preview]
command = "bash scripts/deploy-preview.sh"
icon = "cloud"
